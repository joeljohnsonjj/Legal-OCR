# Google Generative AI SDK
google-genai>=0.2.0
google-generativeai>=0.3.0

# Google Cloud Storage
google-cloud-storage>=2.10.0

# PDF Processing
PyPDF2>=3.0.0
pdf2image>=1.16.3
Pillow>=10.0.0
pycryptodome>=3.19.0
pymupdf4llm>=0.0.21

# OCR
pytesseract>=0.3.10

# Environment Variables
python-dotenv>=1.0.0

# pkg_resources: required by some deps (e.g. gcp-storage-emulator). Setuptools 82+ removed the pkg_resources module — pin below 82.
setuptools>=65.0.0,<82

# JSON Processing (built-in + streaming parse)
# json
ijson>=3.2.0
json-repair>=0.30.0

# Logging (built-in)
# logging

# FastAPI dependencies (new)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP requests (for health checks and streaming)
requests>=2.31.0
httpx>=0.25.0

# Optional: unified LLM interface – set LITELLM_MODEL to switch providers (e.g. gemini/gemini-2.5-flash-lite, azure/gpt-4o-mini)
litellm>=1.50.0

# Vector store for obligation chunks (ChromaDB + optional OpenAI embeddings)
chromadb>=0.4.0
# Dual-track RAG (raw page + obligations) — see legal_rag/ and rag_architecture.md
qdrant-client>=1.9.0
sentence-transformers>=2.2.0
openai>=1.0.0
boto3==1.34.34
python-dotenv==1.0.0

# GCS Emulator (optional, for local development)
# Install from local directory: pip install -e ./gcp-storage-emulator-main
# Or install from PyPI: pip install gcp-storage-emulator
# gcp-storage-emulator>=0.0.0