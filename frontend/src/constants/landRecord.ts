/** Demo land / parcel record id for UI, storage default, and chat `land_record_id`. */
export const DEFAULT_LAND_RECORD_ID = 'LM-DEMO-2847';

export const LAND_ALIAS = 'Riverside Commerce Center';
export const LAND_PRIMARY_USE = 'Corporate campus & retail';

/** Primary brand orange (chat chrome, launcher, primary CTAs). */
export const BRAND_ORANGE = '#EA580C';
/** Darker orange for hover / pressed. */
export const BRAND_ORANGE_HOVER = '#C2410C';
/** Brand blue (links, user bubbles, secondary emphasis). */
export const BRAND_BLUE = '#2563EB';
/** Deeper blue for gradients with orange. */
export const BRAND_BLUE_DEEP = '#0C1B3A';
/** Near-black for text and strong contrast. */
export const BRAND_BLACK = '#0A0A0A';

/** Top header rule / accent (orange). */
export const BRAND_HEADER_ACCENT = BRAND_ORANGE;

/** Primary lease PDF in public/docs (must match backend citations / docId). */
export const DEMO_SAMPLE_LEASE_FILENAME = 'MTNNN.pdf';
export const DEMO_SAMPLE_LEASE_DOC_ID = 'doc-mtnnn-pdf';

/** App shell header title (top bar). */
export const APP_HEADER_TITLE = 'Legal OCR';

/** Chat UI + launcher — single label so FAB and panel stay consistent. */
export const CHAT_ASSISTANT_NAME = 'Legal Document Assistant';
/** Fixed width for chat panel and bottom launcher (no width jump on open). */
export const CHAT_PANEL_WIDTH = 420;

/** User message bubble fill (brand blue). */
export const CHAT_USER_BUBBLE_BG = BRAND_BLUE;

/** Chat surfaces — text and structure (accessible contrast on light greys). */
export const CHAT_TEXT_PRIMARY = BRAND_BLACK;
export const CHAT_TEXT_SECONDARY = '#374151';
export const CHAT_BORDER = '#E5E7EB';
export const CHAT_CARD_BG = '#FFFFFF';
export const CHAT_AREA_BG = '#F3F4F6';

/** Links & clause highlights on assistant bubbles (blue on white). */
export const CHAT_ACCENT_ON_LIGHT = '#1D4ED8';
/** Links & highlights on user (blue) bubbles. */
export const CHAT_LINK_ON_USER_BUBBLE = '#FFFFFF';
