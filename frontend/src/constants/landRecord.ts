/** Hardcoded land record id for UI, storage default, and HEB chat `land_record_id`. */
export const DEFAULT_LAND_RECORD_ID = 'LR-2026-001';

export const LAND_ALIAS = 'Test DDSN';
export const LAND_PRIMARY_USE = 'Corporate Office';

/** H-E-B primary red (header, user bubbles, send control). */
export const HEB_RED = '#DC2626';
/** Hover / pressed state for red interactive elements. */
export const HEB_RED_HOVER = '#DC2626';

/**
 * Legacy header accent; kept for MainTopHeader until a full app rebrand.
 * Prefer {@link HEB_RED} for new surfaces.
 */
export const BRAND_RED = '#C41230';

/** Chat UI + launcher — single label so FAB and panel stay consistent. */
export const CHAT_ASSISTANT_NAME = 'Facility Management Assistant';
/** Fixed width for chat panel and bottom launcher (no width jump on open). */
export const CHAT_PANEL_WIDTH = 420;

/** User message bubble fill (H-E-B red). */
export const CHAT_USER_BUBBLE_BG = '#0077CC';

/** Chat surfaces — text and structure (accessible contrast on white / grey). */
export const CHAT_TEXT_PRIMARY = '#1A1A1A';
export const CHAT_TEXT_SECONDARY = '#555555';
export const CHAT_BORDER = '#E0E0E0';
export const CHAT_CARD_BG = '#FFFFFF';
export const CHAT_AREA_BG = '#F5F5F5';

/** Links & clause highlights on assistant bubbles (dark red on white). */
export const CHAT_ACCENT_ON_LIGHT = '#B3201E';
/** Links & highlights on user (red) bubbles. */
export const CHAT_ACCENT_ON_RED = '#FFFFFF';
